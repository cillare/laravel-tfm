services:
  - type: web
    name: laravel-app
    env: php
    plan: free
    buildCommand: "curl -sS https://getcomposer.org/installer | php && php composer.phar install && php artisan key:generate"
    startCommand: "php artisan serve"
